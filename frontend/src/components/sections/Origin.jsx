export default function Origin() {
  return (
    <section id="origin" className="py-20 px-4 lg:px-8 max-w-4xl mx-auto border-b border-[#23262B]">
      <div className="mb-8">
        <div className="text-xs font-mono text-[#FF1A1A] tracking-wider uppercase mb-1">// CREATOR MANIFESTO</div>
        <h2 className="font-display text-3xl sm:text-4xl font-bold text-white">Origin</h2>
        <div className="w-12 h-1 bg-gradient-to-r from-[#FF1A1A] to-[#3DDAD7] rounded mt-2"></div>
      </div>

      <div className="space-y-6 text-base sm:text-lg leading-relaxed text-gray-300">
        <p>
          Code Crusader isn't a mascot that smiles for the thumbnail. The armor is segmented like a system under load, the visor burns the one red an interviewer's question does, and the twin blades are the only two things sharp enough to cut a hard topic down to what actually matters.
        </p>
        <p>
          Every "watch this in 10 minutes" video skips the part that gets asked in the room: <strong className="text-white">why does it work that way</strong>. This channel doesn't skip that part. Each campaign is the segment order you'd want walking into a product company, GCC, or BFSI interview — not a highlight reel, the actual fight.
        </p>
      </div>
    </section>
  );
}

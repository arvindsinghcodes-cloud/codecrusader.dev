# Base URL for the Spring Boot backend REST API.
# Left empty in dev: Vite's dev-server proxy (see vite.config.js) forwards
# /api/* to VITE_BACKEND_URL. In production, point this at the deployed
# backend, or leave empty to call same-origin /api/* (e.g. behind a reverse proxy).
VITE_API_BASE_URL=/api
